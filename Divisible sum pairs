int sum=0;
        Collections.sort(ar);
        for(int i=0;i<ar.size();i++)
        {
            for(int j=i+1;j<ar.size();j++)
            {
                if((ar.get(i)+ar.get(j))%k==0)
                    sum++;
            }
        }
        return sum;
